task: "oxford_pets_fine_grained"

model_specific_kwargs:
  default:
    pre_prompt: ""
    prompt: "What type of pet is in this photo?"
    post_prompt: ""

generation_kwargs:
  max_new_tokens: 64
  do_sample: False

include: assets/_default_template_yaml
